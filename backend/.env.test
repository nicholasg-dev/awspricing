# Test environment configuration

# AWS Credentials - Mock values for testing
AWS_ACCESS_KEY_ID=test_access_key
AWS_SECRET_ACCESS_KEY=test_secret_key
AWS_REGION=us-east-1

# Server Configuration
PORT=4001
NODE_ENV=test

# MongoDB Configuration - Will be overridden by in-memory MongoDB
MONGODB_URI=mongodb://localhost:27017/aws-pricing-tool-test

# Email Configuration - Mock values for testing
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=test@example.com
SMTP_PASS=test_password
EMAIL_FROM=noreply@example.com

# Scheduler Configuration - Disable in tests
PRICE_UPDATE_SCHEDULE="0 0 31 2 *"  # February 31st (never runs)
ALERT_CHECK_SCHEDULE="0 0 31 2 *"   # February 31st (never runs)
